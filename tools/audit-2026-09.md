# Verification audit — September 2026

The ledger for a full verification pass over BeamerPoint: every element, every feature,
every control. Findings are recorded here as they are found, **catalogued before any is
fixed**, then fixed in severity order, each with the test that would have caught it.

A finding is marked Fixed, never deleted, so the file stays a record of what was wrong.

## Severity

| | Meaning |
| --- | --- |
| **S1** | Breaks the PDF — no output at all |
| **S2** | Silent data loss or structural loss — the model or the file quietly stops saying what the user made |
| **S3** | Wrong on the canvas — the editor disagrees with the PDF |
| **S4** | Inert control — something that moves and changes nothing |
| **S5** | Cosmetic, misleading text, or dead code |

## Why this audit exists

Before a line of it was written, the suite's central assertion was shown not to be able to
fail the way it is supposed to. The round trip — emit, parse, emit — is what makes the
LaTeX source a first-class editable view, and it was checked with byte fixpoint plus
`guard.ok`. Both hold for a deck the parser understood **none** of, because a `raw` element
re-emits its captured bytes verbatim. `health.demoted` does not close the gap either: it
counts only what the guard demoted, and a recognizer that declines produces raw the guard
never sees (`parse/guard.ts` returns early for anything already raw).

The fix is `packages/core/test/helpers/roundTrip.ts`, which adds the whole-document
fixpoint (the only signal covering the preamble) and a **census** of what the deck is made
of. F-007 below is what it found on its first run.

## Coverage baseline

Measured with `npm run test:coverage` (v8, lines) after stages 0 and 2, before any new
module tests. `all: true`, so a file nothing imports counts as 0% rather than vanishing.

| Package | Lines | Notes |
| --- | --- | --- |
| core | **92.1%** of 6,226 | Softest: `emit/escape.ts` 83% (Invariant 2), `parse/reid.ts` 84% (id stability), `emit/elements.ts` 85%, `model/shapeOps.ts` 78%, `model/chartOps.ts` 71% |
| engine | **24.9%** of 301 | Only `packageIndex.ts` is tested. `logParser`, `buildProject`, `attachDiagnostics` at 0% |
| app | **12.8%** of 8,578 | Every panel and canvas view at 0% — those are the mouse pass's job. The non-UI modules at 0% are the unit targets: `persist`, `fileLink`, `exportProject`, `images`, `resources`, `domSelection`, `textSelection`, `pointerDrag`, `applyFormat`, `mathPreview`, `useEngine`. `store.ts` at 46% |

The browser-only `backends/busytex/**` is excluded — it is covered by the engine runner.

## Findings

| ID | Stage | Area | Sev | Symptom | Evidence | Status | Fix |
| --- | --- | --- | --- | --- | --- | --- | --- |
| F-001 | planning | code blocks | S1 | A `minted` code block produces **no PDF at all**. It is offered in the Code panel's "Typeset with" picker. | Engine: `ok: false`, 0 bytes, `! Package minted Error: Missing definition for highlighting style "default"`. | **Fixed** | Preview compiles minted with listings; engine case passes, mouse-verified |
| F-002 | planning | emitter | S5 | `EmitOptions.target` is read by nothing. Six call sites pass it; its doc comment explains the non-use by naming `instrument.ts`, which does not exist anywhere in the repo. | `grep` for any read of the option: none. `find` for `instrument*`: none. | **Fixed** | `EmitOptions.target` deleted with its six call sites |
| F-003 | planning | docs | S5 | docs/ENGINEERING.md says minted preview "falls back to `listings` with a warning". There is no fallback — `buildProject.ts` only pushes a warning string. | Code read, and F-001's engine result. | **Fixed** | docs/ENGINEERING.md corrected |
| F-004 | planning | engine UI | S3 | `useEngine.ts` builds the project, discards `project.sourceMap`, and re-emits the deck to get another. Harmless while preview = export; once F-001's fallback makes them differ, every diagnostic maps to the wrong slide. | Code read. | **Fixed** | `useEngine` maps diagnostics through `project.sourceMap` |
| F-005 | planning | engine UI | S4 | Every build warning is mapped to `severity: 'error'`, so an informational note would mark a successful compile as failed. | Code read. | **Fixed** | Build notes are `info`; a fallback compile reads "Compiled cleanly" |
| F-006 | planning | engine | S5 | `CompileResult.passesRun` is fabricated: `passes === 'auto' ? 2 : Number(passes)` regardless of what ran; the backend receives only a boolean `rerun`. | Code read, `BusytexEngine.ts`. | Open | |
| F-022 | stage 4 | picture crop | S4 | **Cropping a picture in its default (flow) placement uses handles that are not on the picture and do not follow the mouse.** A flow picture's element box spans the whole text column, and the crop edges are drawn on that box, not on the image: with a left-aligned 0.6-textwidth picture the east crop edge floats about 200 CSS px to the right of the picture's real edge, over empty slide. Dragging it 65 CSS px — 22 % of the picture's width — crops 20.6 bp of 320 (6 %), about 1/3.4 of the drag, and the edge stays where it was. It is the diagram lesson in docs/ENGINEERING.md ("ask the element, not the rect it was drawn into") recurring in the crop overlay. | Browser pane, mouse: picture dropped on the canvas, Crop clicked; `.bp-crop-e` at x 274 before and after the drag while the `<img>` ends at x 200; `trim=0bp 0bp 20.6bp 0bp` from a 65-px drag. | Open | |
| F-021 | stage 4 | source panel | S5 | **Auto-apply moves an end-of-line comment away from its line.** Typing `% hand edit` at the end of the `\title{…}` line and pausing: 800 ms later auto-apply rewrites the buffer and the comment is now on its own line after `\date{…}`, followed by an extra blank line. LaTeX does not care; the reader does, and the text jumps under the cursor of someone who has only paused. Byte preservation is promised for what the app does not understand; this keeps the bytes but not their place. | Browser pane, keyboard in CodeMirror; the preamble before and after. | Open | |
| F-020 | stage 4 | layout | S5 | **Below about 1,000 pixels wide the whole page scrolls sideways and the format pane falls off the screen.** The columns' minimum widths add up to 998 px and nothing adapts: at an 800-px window (half a laptop screen) only 32 px of the format pane is visible, so every property control is reachable only by scrolling the page horizontally. `.bp-app` sets no minimum and no responsive rule. | Browser pane at 800×475: `documentElement.scrollWidth` 998; format pane spans x 768–998. | Open | |
| F-019 | stage 4 | canvas selection | S4 | **Once a text box or a table is selected, its text cannot be clicked into.** The selection overlay's move bands are a constant 10 screen pixels thick (the scale-compensation fix), but at the canvas's usual zoom a line of text or a table row is 7–10 pixels tall, so the north and south bands meet over it. A freshly inserted table is selected, so its header row — exactly what a user types first — is under the north band, and clicking a header cell starts a move instead of placing a caret; typed text goes nowhere. A selected one-line text box is covered end to end. Clicking the grey stage around the slide does not deselect, so the way out is to select some OTHER element first. | Browser pane, mouse, 1280×760: `elementFromPoint` at the centre of the first cell's text → `bp-move-band-n`; at the middle and left third of a selected 10-px text box → `bp-move-band-s`. With the list selected instead, the same header click focuses `bp-cell-body` and typing lands. | Open | |
| F-018 | stage 4 | presentation metadata | **S2** | **Typing into a Presentation field silently destroys any LaTeX in it.** Title, subtitle, author, institute and date are edited as PLAIN text: the field shows `richTextToPlain(value)`, in which a raw island is empty, and writes back plain text. So `\author{Alice Smith \and Bob Jones}` shows as `Alice Smith  Bob Jones` (the `\and` already invisible), and adding " Jr." saves `\author{Alice Smith  Bob Jones Jr.}` — one author where there were two, on the title page and in every footline. The default deck's own `\date{\today}` is stored as a raw island, so its Date field looks EMPTY (with `\today` only as grey placeholder text) and the first keystroke replaces it. `\and` is how every multi-author Beamer deck is written; `\inst{…}`, `\\` line breaks and `\today` go the same way. | Browser pane, keyboard: author field value `Alice Smith  Bob Jones`; End, type " Jr.", Tab → `\author{Alice Smith  Bob Jones Jr.}`. Date: field value `""` with placeholder `\today`; typing " in Paris" → `\date{ in Paris}`. | **Fixed** | A field holding anything but plain words is shown and edited AS LaTeX (badge: LaTeX), read back by core `richTextFromTex`, which is byte-exact or refuses; refused text (unbalanced brace mid-typing, bare `&`, a comment) stays in the field outlined red and is not committed. Plain fields still escape. Same component now serves the slide title, which had the same flaw. Keyboard: `uthor{Alice Smith nd Bob Jones Jr.}`; Date shows `	oday`. |
| F-017 | stage 4 | deck font / canvas | S3 | **Choosing a deck font changes the PDF and never the canvas.** Design ▸ Font family writes the right LaTeX — Palatino gives `\usepackage{mathpazo}` with `\usefonttheme{serif}`, the measured requirement — but every theme's canvas spec hardcodes `fontFamily: 'sans'` (`themes.ts`), `resolveTheme` looks only at the theme name, and nothing reads the font package or the font theme. So the editor keeps drawing Latin Modern Sans while the PDF is Palatino (or Times, Charter, Helvetica…), including the title page, which has the same switch. | Browser pane, with the mouse: Font family → Palatino; source correct; computed `font-family` of the paper, the body, text, list items and the frame title all still "Latin Modern Sans". Code: `fontFamily: 'sans'` is the only value any theme spec sets. | **Fixed** | core `canvasFontStack(preamble)` applies the measured `\familydefault` rule (serif package alone stays sans; serif font theme alone is CM Roman) and maps each of the 11 families to a CSS stack; used by the paper, thumbnails and title page. Mouse: Design ▸ Font family → Palatino: paper, title page and thumbnail all resolve to the Pagella/Palatino stack. |
| F-016 | stage 4 | crash recovery | S4 | **A false "Unsaved changes were found" banner on every launch until the user edits something — and Discard does not make it go away.** Autosave writes the structured deck to IndexedDB only when the deck CHANGES, but on `pagehide` it writes the `.tex` mirror whenever the deck is not the last-saved one — which, before any save, is always. So a user who opens the app, looks, and leaves is told on the next visit that the tab "probably closed before they were written". Clicking **Discard** hides the banner but leaves the mirror in localStorage, so it returns on the very next load. Distinct from F-010, which is about emitter changes between builds. | Browser pane, with the mouse: banner on a plain reload; `bp:deck:current` absent from IndexedDB while the mirror is present; Discard → reload → banner again; one edit → "Saved just now" and the deck lands, ruling out a broken IndexedDB. | **Fixed** | The mirror now exists only while IndexedDB is behind it: `markClean` marks the session's starting deck (restored or default) as saved, a successful save removes the mirror (unless the deck moved on mid-write), and Discard and Recover remove it. Mouse: two plain reloads, no banner, no mirror; an edit then an immediate reload shows the banner; Discard clears it and the next reload is clean. |
| F-015 | stage 1 | picture | S2 | **A transparent picture with a caption, or with ANY explicit alignment (the panel's Left, Centre and Right buttons all set one), turns into an uneditable raw block on the next reparse.** Both controls sit side by side in the Picture panel. The emitted LaTeX is intact and compiles, but the recognizer declines the combination, so after a source edit, a reload from `.tex` or an import the picture is raw: its transparency, caption and alignment can no longer be changed from the panel. (The raw block also loses its indentation — cosmetic.) Transparency with crop or rotation is fine. Separately, `imageOpacity.spec.ts` has a test named "keeps the crop, rotation and **caption** of a faded picture" that builds no crop and no caption — it covers rotation only, and its name claims exactly the combination that fails. | Property counterexample, then isolated: `opacity + caption` → `raw`, `opacity + align right` → `raw`, and `opacity + align center` → `raw` (a first guess that centre was safe was wrong); `opacity + crop`, `opacity + rotate`, `caption + crop` all survive. | **Fixed** | `recognizeGraphic` lets the figure and alignment recognizers take the one-node transparent `tikzpicture` as their graphic; the misnamed test renamed and real caption and alignment cases added; property exclusion removed, 3000-deck soak clean |
| F-014 | stage 1 | text / emitter | **S2** | **Two text boxes on a slide become one paragraph — in the PDF, and then in the model.** Adjacent flow text elements are emitted with a single newline between them, which LaTeX reads as a space. The PDF sets them on one line as one paragraph while the canvas draws two boxes; and the next reparse (a source edit, a reload from `.tex`, an import) merges them into ONE element permanently. Adding a second text box is one of the commonest things a user does. Every existing signal is clean — bytes, `guard.ok`, `demoted: 0`, `fixpoint`, `rawRatio: 0` — only the census sees it. The `tabular` lesson in docs/ENGINEERING.md is the same failure, fixed for tables and never for text. | Found by the fast-check property after 541 random decks, shrunk to two words. Engine: `FIRSTPARA SECONDPARA` in one text item at one baseline (y=132). Pinned as `it.fails` in `property.spec.ts`. | **Fixed** | `emitChildren` puts a paragraph break between flow texts, through a pause; PDF: two baselines (137 / 124) |
| F-013 | stage 2 | log parser | S5 | **Every diagnostic is reported twice, and every compile carries a spurious warning.** busytex's log is a concatenation of sections — `TEXMFLOG`, `LOG`, `STDOUT`, `STDERR` — and `LOG` and `STDOUT` both carry each message, so `parseLog` reports each error and warning twice. A plain two-line deck reports `Shell escape feature is not enabled.` twice on every compile: noise in the Log tab that trains a user to ignore warnings. *Checked and ruled out:* that warnings from an earlier pass (e.g. an undefined reference the second pass resolves) leak through — a `\label`/`\eqref` deck at `passes: 'auto'` reported none. Confirmed in the Log tab: every item listed twice, and the status bar counts one problem as "5 errors". **Also:** the first copy of a wrapped error swallows TeX's help text and memory statistics into its message — the Log tab shows the minted error followed by "Pretend that you're Hercule Poirot… Here is how much of TeX's memory you used: 25370 strings…", because re-joining 79-column lines does not stop at the end of the error. | `logParser.spec.ts` against the captured minted log, `it.fails` "reports each error once"; live compile and the Log tab, with the mouse. | Open | |
| F-012 | stage 2 | build | S2 | **A missing `.bib` is never reported, so every citation silently becomes `[?]`.** `buildProject` warns about a missing resource only when `tex.includes(res.path)` — but the path is `refs.bib` and the document says `\bibliography{refs}`, without the extension. Images are unaffected because their path appears verbatim. | `buildProject.spec.ts`, `it.fails` "warns about a missing .bib the reference list asks for". | **Fixed** | `isReferenced` reads `ibliography{…}`'s arguments; an unused `.bib` still is not reported |
| F-011 | stage 2 | export | S5 | **Export and "Save PDF" file names drop every non-ASCII letter.** `baseName` strips with `/[^\w -]+/g`, and `\w` is ASCII-only: *Présentation über Physik* exports as `Prsentation-ber-Physik.tex`, and a title wholly in a non-Latin script (Persian, Greek, Chinese) becomes `presentation`. | `exportProject.spec.ts`, `it.fails` "keeps accented and non-Latin letters in the file name" — received `Prsentation-ber-Physik.tex`. | Open | |
| F-010 | stage 2 | crash recovery | S5 | **A false "unsaved changes" prompt after any update that changes the emitter's output.** On launch, `App.tsx` offers recovery whenever the stored `.tex` mirror differs from `emitDeck(savedDeck)` — but the mirror was emitted by the *previous* build. If a release changes how an existing deck emits, the two differ with no edit at all, and the user is asked to choose between "their" work and "their" work. Comparing against a mirror timestamp, or storing the emitted text beside the deck, would avoid it. | Code read (`App.tsx` restore effect, `persist.ts` `saveNow`). The persistence layers themselves are pinned in `persist.spec.ts`. | **Fixed** | Fixed with F-016: a mirror is removed as soon as the deck it mirrors is saved, so one found at launch means unsaved edits and no longer depends on comparing texts emitted by two builds. The text comparison remains as a guard, and an equal mirror is now cleared. |
| F-009 | stage 2 | undo / source panel | S2 | **Undo throws away unapplied source edits.** `mutate` and `beginGesture` refuse to run while the source panel is dirty (the canvas is locked), but `undo()` and `redo()` do not check, and the ribbon's Undo/Redo buttons have no `disabled={locked}`. Type in the source panel, click Undo on the ribbon, and the deck reverts *and* the source text is regenerated over the typing — gone, with no prompt. Ctrl+Z inside the editor is safe (it is left to CodeMirror); the ribbon button is not. | `history.spec.ts`, `it.fails` "does not throw away unapplied source edits when Undo is pressed". | **Fixed** | `undo`/`redo` obey the canvas lock; ribbon buttons disabled while locked; mouse-verified |
| F-008 | stage 3 | engine / PDF tab | S3 | A **failed** compile returns `pdf: null`, breaking `CompileResult`'s own `pdf?: Uint8Array` contract: `BusytexEngine` spreads `pdf` whenever it is `!== undefined`, so `null` passes through. `PdfPanel` guards with `pdf === undefined` and then calls `pdf.slice()`, which throws — so after a failed compile the PDF tab presumably shows a raw JS error and keeps the previous PDF's pages on screen. The engine's own `ok` computation would also throw on `success: true, pdf: null`. | Harness: a minted compile came back with `pdf === null`. **Confirmed with the mouse in stage 4**: compile a good deck (2 pages render), switch the code block to minted, compile — the PDF tab shows "Compilation failed", the raw error `Cannot read properties of null (reading 'slice')`, AND the two stale pages of the previous PDF underneath it. | **Fixed** | `toCompileResult` (engine, pure, tested) drops a null or zero-length pdf and synctex; `PdfPanel` guards `== null`, cancels any in-flight render and clears the stale pages and error. Mouse: good compile → 2 pages; add `nonesuchpkg`, compile → "Compilation failed", 0 pages, no JS error. |
| F-007 | stage 0 | bibliography | S2 | A reference list's size is lost on every reparse. The emitter writes `\footnotesize` on its own line before `\bibliography{...}`; the parser does not fold it back into `sizeHint`, so it returns as an **invisible text element** holding a bare `sym`, beside a bibliography with no `sizeHint`. Deleting that invisible element silently changes the reference list's size. Every slide made by *References slide* is affected. | Census: `[bibliography, text] → [bibliography, text, text]`. Bytes identical, `guard.ok`, `demoted: 0` — which is why every existing test passed. | **Fixed** | `readBibliography` reads size, style and `ibliography` back as one element; the sibling two-element style bug fixed with it |
